// hero.ts
import { heroui } from "@heroui/react";
export default heroui({
  themes: {
    dark: {
      colors: {
        background: "#000000",
        foreground: "#ffffff",
      },
    },
  },
});
